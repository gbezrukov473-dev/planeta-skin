# Руководство по миграции HTML файлов на шаблоны

## ✅ Что уже сделано

- ✅ `index.html` - полностью мигрирован
- ✅ Шаблоны созданы (`templates/header.html`, `templates/footer.html`)
- ✅ Vite плагин настроен с поддержкой подсветки активной страницы
- ✅ Плагин автоматически определяет текущую страницу и подсвечивает активный пункт меню

## 📋 Инструкция по миграции остальных файлов

### Шаг 1: Найдите начало header

В каждом HTML файле найдите начало header. Обычно это:
- `<!-- === ПРЕЛОАДЕР === -->` или
- `<!-- === ШАПКА === -->` или  
- `<div id="preloader"`

### Шаг 2: Найдите конец header

Header заканчивается на:
- `</header>`

### Шаг 3: Замените header на плейсхолдер

Замените весь блок от начала прелоадера до `</header>` включительно на:

```html
    <!-- HEADER_PLACEHOLDER -->
```

**Важно:** Сохраните отступы (обычно 2 или 4 пробела в начале строки).

### Шаг 4: Найдите footer

Footer обычно начинается с:
- `<footer id="contacts"` или
- `<!-- === ПОДВАЛ === -->`

### Шаг 5: Замените footer на плейсхолдер

Замените весь блок от `<footer>` до `</footer>` включительно, а также следующий за ним тег `<script type="module" src="/src/main.js"></script>` на:

```html
    <!-- FOOTER_PLACEHOLDER -->
```

## 📝 Пример миграции

### До:
```html
<body class="bg-slate-50">
  <!-- === ПРЕЛОАДЕР === -->
  <div id="preloader">...</div>
  
  <!-- === ШАПКА === -->
  <header>...</header>
  
  <main>
    <!-- Контент страницы -->
  </main>
  
  <footer>...</footer>
  <script type="module" src="/src/main.js"></script>
</body>
```

### После:
```html
<body class="bg-slate-50">
  <!-- HEADER_PLACEHOLDER -->
  
  <main>
    <!-- Контент страницы -->
  </main>
  
  <!-- FOOTER_PLACEHOLDER -->
</body>
```

## 🎯 Файлы для миграции

Следующие файлы требуют миграции:

- [ ] `about.html`
- [ ] `specialists.html`
- [ ] `equipment.html`
- [ ] `promo.html`
- [ ] `reviews.html`
- [ ] `certificates.html`
- [ ] `laser.html`
- [ ] `removal.html`
- [ ] `hardware.html`
- [ ] `inject.html`
- [ ] `aesthetic.html`
- [ ] `body.html`
- [ ] `thanks.html`
- [ ] `policy.html`

## ✨ Автоматическая подсветка активной страницы

После миграции плагин автоматически:
- ✅ Определяет текущую страницу по имени файла
- ✅ Подсвечивает активный пункт в верхнем меню (десктоп)
- ✅ Подсвечивает активный пункт в меню услуг (второй уровень)
- ✅ Подсвечивает активный пункт в мобильном меню

**Никаких дополнительных действий не требуется!**

## 🔍 Проверка после миграции

1. Запустите сборку: `npm run build`
2. Проверьте, что header и footer вставляются корректно
3. Проверьте, что активная страница подсвечивается в меню
4. Проверьте работу на разных страницах

## ⚠️ Важные замечания

1. **Не удаляйте контент между header и footer** - заменяйте только сами header и footer
2. **Сохраняйте отступы** - используйте те же отступы, что были в оригинале
3. **Проверьте закрывающие теги** - убедитесь, что не осталось лишних `</header>` или `</footer>`
4. **Лайтбокс** - если на странице есть лайтбокс, он должен остаться (он уже включен в footer шаблон)

## 🐛 Решение проблем

### Проблема: Header/footer не вставляются
- Проверьте, что плейсхолдеры написаны точно: `<!-- HEADER_PLACEHOLDER -->` и `<!-- FOOTER_PLACEHOLDER -->`
- Убедитесь, что файлы шаблонов существуют в `templates/`

### Проблема: Активная страница не подсвечивается
- Проверьте, что имя файла соответствует маппингу в `vite-plugin-html-template.js`
- Убедитесь, что файл собран через `npm run build`

### Проблема: Дублирование header/footer
- Убедитесь, что старый header/footer полностью удален
- Проверьте, что плейсхолдеры не дублируются

## 📚 Дополнительная информация

- См. `TEMPLATE_USAGE.md` для подробной информации о шаблонах
- См. `vite-plugin-html-template.js` для понимания работы плагина
